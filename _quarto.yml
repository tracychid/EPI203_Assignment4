project:
  type: manuscript

manuscript:
  article: index.qmd

format: 
  pdf:
    geometry:
      - margin=0.75in
  html:
    comments:
      hypothesis: true
  docx: default
  jats: default

  # (other formats)
  # pdf: default

execute:
  freeze: true

editor: visual

